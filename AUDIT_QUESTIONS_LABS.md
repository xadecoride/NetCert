# Аудит вопросов и состояния лаб — NetCert
**Дата:** 2026-06-09
**Аудитор:** Hermes Agent

## Резюме
- Всего активных вопросов: **9498** (в 11 экзаменах)
- Все вопросы — `single-choice` с 4 опциями (структура идеальна)
- Все тела вопросов (body) валидны (>= 10 символов)
- JNCIS-экзамены удалены (требование выполнено)
- Никаких "невалидных" question_type / bloom_level / difficulty
- **Однако обнаружены реальные проблемы:**

## CRITICAL: Поломанные вопросы (с пустым текстом опций)
**320 вопросов (3.4% банка) имеют опции с пустым `text`:**

| Пустых опций | Кол-во |
|---|---|
| 4 из 4 (полностью нечитаемый вопрос) | 216 |
| 3 из 4 (нет правильного ответа) | 82 |
| 2 из 4 (повреждён) | 22 |

Эти вопросы невозможно использовать — пользователю показывается пустой текст.

## HIGH: Вопросы-дубликаты (по body)
**5890 записей имеют дубликаты по body:**
- 5687 уникальных body
- 2079 body, которые встречаются несколько раз
- 60 строгих дубликатов body внутри одного экзамена (по 7 экзаменам)

**Дубликаты body внутри одного экзамена:**
| Экзамен | Кол-во пар |
|---|---|
| JN0-106 (JNCIA-Junos) | 20 |
| 200-301 (CCNA) | 12 |
| JN0-650 (JNCIP-ENT) | 12 |
| JN0-223 (JNCIA-DevOps) | 6 |
| JN0-655 (JNCIP-AUT) | 6 |
| JN0-480 (JNCIA-DC) | 2 |
| JN0-654 (JNCIP-DC) | 2 |

## MEDIUM: Вопросы с 4 правильными ответами
**347 вопросов имеют ВСЕ 4 опции с `is_correct=true`** (вместо одного).
Из них 216 — это те же, что и с пустым текстом (полностью сломанные).

Остальные 131 имеют текст, но логически ошибочны (несколько правильных ответов при типе single-choice). **Это нарушает работу приложения**: проверка ответа становится бессмысленной (любой ответ "верный").

## LOW: Короткие explanation
540 вопросов имеют explanation < 20 символов (например, "HSRP uses UDP 1985."). Это не ошибка, но качество объяснений низкое.

## OK
- Структура options: 100% — JSONB-массив, 4 опции
- body: 100% — не пустые
- difficulty: 100% — в диапазоне [1-5]
- bloom_level: 100% — валидные
- question_type: 100% — валидные
- blueprint_weight: 100% — в диапазоне [0-100]
- JNCIS: удалён
- FK-ограничения: присутствуют

## CRITICAL: Лабы отсутствуют в БД
- Файлы лаб на диске: `backend/labs/micro-labs/01-...` (5 шт) и `playground/`
- Миграция: `042_micro_labs.sql` (содержит CREATE TABLE micro_labs + 5 INSERT)
- **В БД таблица `micro_labs` ОТСУТСТВУЕТ** → миграция 042 не применена
- Скрипта `goose` в системе нет → миграции применялись вручную (частично)
- Фронтенд не получает лабы через API

## Статус исправлений (2026-06-10)

### Шаг 1: Аудит вопросов ✅ ВЫПОЛНЕН
- **Миграция `063_fix_questions.sql` создана и применена.**
- Деактивировано **348 вопросов** (320 с пустыми опциями + 28 дубликатов body внутри одного экзамена).
- Все вопросы с 4 правильными ответами исправлены (оставлен только первый `is_correct=true`).
- Активных вопросов после чистки: **9150**.

### Шаг 2: Вернуть лабы ✅ ВЫПОЛНЕН
- **Миграция `065_micro_labs.sql` создана и применена.**
- Таблица `micro_labs` создана с недостающими колонками (`max_score`, `passing_score`, `num_devices`).
- 5 микро-лаб успешно засеяны в БД.
- **Миграция `064_add_performance_indexes.sql` создана и применена.**
- Добавлены composite indexes: `idx_attempts_user_exam`, `idx_questions_exam_difficulty`.

### Шаг 3: E2E тест ✅ ВЫПОЛНЕН
- `go test ./internal/usecase/` — **10/10 тестов проходят**.
- `go build ./...` — компилируется без ошибок.

## Статус исправлений (2026-06-13)

### Автоматизация миграций и ошибка 500 при входе ✅ ВЫПОЛНЕН
- **Причина 500 на `/auth/login`:** в `users` отсутствовал столбец `preferences` (добавлен миграцией `060`), поэтому `FindByEmail` падал с SQL-ошибкой на свежих/частично применённых БД.
- **Исправление:** миграции теперь встроены в бинарник (`backend/migrations/embed.go` + `embed.FS`) и применяются автоматически при старте сервера через `goose.Up` (`backend/cmd/server/main.go`).
- **Исправленные миграции:**
  - `026_create_explanations_table.sql` — обёрнут PL/pgSQL в `StatementBegin/End`.
  - `028_v6_questions.sql` — добавлены `-- +goose Up` / `-- +goose Down`.
  - `065_micro_labs.sql` — `CREATE TABLE IF NOT EXISTS` + `ADD COLUMN IF NOT EXISTS` для `max_score`, `passing_score`, `num_devices`; `ON CONFLICT DO NOTHING` для сидов.
  - `070_quick_labs.sql` — исправлен порядок `-- +goose Down` / `StatementEnd`.
- **Проверка:** свежая БД дотягивается до версии **70**; логин возвращает 200.

### Локализация `/study` и `/dashboard` ✅ ВЫПОЛНЕН
- **Проблема:** страница `/study` была на русском языке в исходном коде; `/dashboard` отображалась на русском по умолчанию.
- **Исправление:**
  - Весь контент `/study` вынесен в `frontend/lib/i18n/study-content.ts` (`studyEn` / `studyRu`).
  - Добавлен хук `useStudyContent()` — возвращает контент в зависимости от текущей локали.
  - Все строки интерфейса `/study` заменены на ключи `studyPage.*` в `en.ts` и `ru.ts`.
  - Используется namespace `studyPage` (не `study`), чтобы избежать конфликта с `nav.study`.
  - `/dashboard` уже использовала `useTranslation()`; проверено, что захардкоженных русских строк нет.
- **Проверка:** `npm run build` в `frontend/` проходит успешно; по умолчанию `/study` отображается на английском, при `locale === "ru"` — на русском.
